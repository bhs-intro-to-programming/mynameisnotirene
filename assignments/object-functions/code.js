const getX = (object) => {
  return object.x
}
const point = (x, y) => {
  return {x,y}
}
const emptyObject = (object) => {
  return {}
}
const distance = (x, y) => {
  return 
}
const midpoint = (x1, x2) => {
  return x1/x2
}
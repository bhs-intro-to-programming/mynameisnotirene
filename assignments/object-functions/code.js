const getX = (object) => {
  return object.x
}
const point = (x, y) => {
  return {x,y}
}
const emptyObject = (object) => {
  return {}
}
const distance = (x1, y2) => {
  return 
}
const midpoint = (x1, x2) => {
  return (x1.x + x2.x) / (x1.y + x2.y)
}